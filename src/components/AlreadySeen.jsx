import React from 'react';

const AlreadySeen = () => {
  return (
    <div className="already-seen">
      <span>
        You have already seen this manuscript while classifying. Don&apos;t worry,
        we are constantly adding new manuscripts to the site. You can still annotate
      </span>
    </div>
  );
};

export default AlreadySeen;
